{
	"name": "mikro-orm-issue",
	"version": "0.0.1",
	"private": true,
	"scripts": {
		"test": "vitest run",
		"db-setup": "npx dbmate -u $TEST_DATABASE_URL drop && npx dbmate -u $TEST_DATABASE_URL --no-dump-schema --wait up"
	},
	"dependencies": {
		"@mikro-orm/core": "^5.7.14",
		"@mikro-orm/postgresql": "^5.7.14",
		"pg": "^8.11.2"
	},
	"devDependencies": {
		"dbmate": "^2.5.0",
		"typescript": "^5.0.0",
		"vitest": "^0.34.0"
	},
	"type": "module"
}
